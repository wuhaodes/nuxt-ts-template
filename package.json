{
  "name": "mm-site-spa-maker",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "dev": "HOST=0.0.0.0 PORT=8002 nuxt-ts",
    "start": "HOST=0.0.0.0 nuxt-ts start",
    "clean": "rm -rf dist*",
    "build": "yarn clean && NODE_ENV=production nuxt-ts build",
    "ci-build": "coa-ci build"
  },
  "dependencies": {
    "big.js": "^5.2.2",
    "coa-nuxt": "^1.0.30",
    "element-ui": "^2.13.2",
    "vue-drag-resize": "^1.4.1",
    "vue-draggable-resizable-gorkys": "^2.4.3"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^2.0.2",
    "sass": "^1.26.10",
    "sass-loader": "^9.0.2"
  }
}
